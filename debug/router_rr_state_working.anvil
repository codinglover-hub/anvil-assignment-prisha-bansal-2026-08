proc rr_test() {
    reg rr0 : logic;

    loop {
        let next_rr = if (*rr0 == 1'b0) {
            1'b1
        } else {
            1'b0
        } >>

        set rr0 := next_rr >>

        cycle 1
    }
}
